name: Legacy My Resumes installer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — my-resumes-manager-v4.js owns this feature."
