name: JobAI button tone

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  change-button-tone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Legacy manual-only workflow
        run: echo "Profession button styling is already integrated. No automatic changes are needed."
